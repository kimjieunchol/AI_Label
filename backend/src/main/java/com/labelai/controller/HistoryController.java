package com.labelai.controller;

import org.springframework.web.bind.annotation.*;

/**
 * 이력 관리 API
 * - GET /api/history : 이력 목록 조회 (페이징)
 * - DELETE /api/history : 이력 일괄 삭제
 */
@RestController
@RequestMapping("/api/history")
public class HistoryController {
    // 이력 조회 및 삭제
}
